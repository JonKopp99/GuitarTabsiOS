Change ViewController Name to TabBarVC
Add Better Comments





PLAY feature idea
so
measured in BPM
use timer
divide BPM/60
for every second  have the x^ amount of notes be played!

So if BMP is 120 2 notes are played every second.

maybe every line is equal to ~5- 10 seconds of the song.



func addSpacing()
{
let spacing = spacingBetweenNotes()
var spacing2 = spacingBetweenNotes()
spacing2.removeLast()
spacing2.removeLast()
for i in theSong
{
for k in 0 ..< i.theE.count - 1
{
if(i.theE[k].count <= 1)
{
i.e = (i.e + i.theE[k] + spacing)
}else
{
i.e = (i.e + i.theE[k] + spacing2)
}

if(i.theA[k].count <= 1)
{
i.a = (i.a + i.theA[k] + spacing)
}else
{
i.a = (i.a + i.theA[k] + spacing2)
}

if(i.theD[k].count <= 1)
{
i.d = (i.d + i.theD[k] + spacing)
}else
{
i.d = (i.d + i.theD[k] + spacing2)
}

if(i.theG[k].count <= 1)
{
i.g = (i.g + i.theG[k] + spacing)
}else
{
i.g = (i.g + i.theG[k] + spacing2)
}

if(i.theB[k].count <= 1)
{
i.b = (i.b + i.theB[k] + spacing)
}else
{
i.b = (i.b + i.theB[k] + spacing2)
}

if(i.theEE[k].count <= 1)
{
i.ee = (i.ee + i.theEE[k] + spacing)
}else
{
i.ee = (i.ee + i.theEE[k] + spacing2)
}
}
i.e = (i.e + i.theE[i.theE.count-1])
i.a = (i.a + i.theA[i.theE.count-1])
i.d = (i.d + i.theD[i.theE.count-1])
i.g = (i.g + i.theG[i.theE.count-1])
i.b = (i.b + i.theB[i.theE.count-1])
i.ee = (i.ee + i.theEE[i.theE.count-1])
}
}
add right swipe for pageFlop

location's
0 = e
1 = a
2 = d
3 = g
4 = b
5 = ee
6 = theSong
7 = theArtist
8 = theDifficulty
9 = anything else
